# Dependencies
node_modules/
.pnp/
.pnp.*

# Build outputs
dist/
build/
out/
dist-server/
.vercel/
*.tsbuildinfo

# Env / secrets  (NOTE: .env.local holds the Vercel token — never commit)
.env
.env.*
!.env.example

# Local runtime data / databases
*.sqlite
*.sqlite-*
*.db
.bothread/

# Parallel-agent worktrees (never commit these — they're isolated scratch checkouts)
.claude/worktrees/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/

# Test artifacts
coverage/
playwright-report/
test-results/
.evidence/screenshots/*.tmp.png

# Scratch screenshots / scratch evidence at repo root
/*.png

# Internal build journal & plan (kept local, not published)
CLAUDE.md
PLAN.md
VIRAL_HANDOFF.md

# Local launch/marketing drafts (kept local, not published)
launch-md-files/
launch_playbook_v2.md
ways-to-improve-bothread.md

# Local raw video/gif exports (kept local, not published — docs/bothread-demo.gif is the one that IS published)
/bothread-demo.gif
/bothread-short-audio.mp4
/bothread-short-graphic.mp4

.vercel
